apiVersion: v1
kind: Service
metadata:
  name: pizzaappfrontend-service
  namespace: junction
spec:
  selector:
    app: pizzaappfrontend
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP